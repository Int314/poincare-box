node_modules
.wrangler
dist

# env (dotenvx 運用)
# .env.local は暗号化済みのため git にコミット OK
# .env.keys (復号鍵) は絶対にコミットしない
.env
.env.*.local
.env.keys

# Generated by `wrangler types`
worker-configuration.d.ts

# 一時ファイル（調査スクリプト・スクショ・ログ等。CLAUDE.md グローバル規約準拠）
tmp/*
!tmp/.gitkeep

# Claude Code タスク管理（ローカル専用・グローバル規約）
tasks/

# OS
.DS_Store
